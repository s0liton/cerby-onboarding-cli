# Build artifacts
*.egg-info/
src/*.egg-info/
cerby_onboarding_cli.egg-info/
cerby_rotation_tool.egg-info/

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/

# IDE / OS
.vscode/
.DS_Store

# Virtual environments
.venv

# Local service config (may contain access_token bootstrap)
config/config.yaml

# Important persistent state (keep assets/.gitkeep)
assets/.cerby_session.json
assets/work_sessions/

# User-exported reports (project root)
cerby_run_report_*.json
cerby_work_session_*.json

# Service live report (project root)
running_report.json

# Runtime logs
log/
